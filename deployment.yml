apiVersion: apps/v1
kind: Deployment
metadata:
  name: front
  namespace: <NAMESPACE>
spec:
  replicas: 1
  selector:
    matchLabels:
      app: front
  template:
    metadata:
      labels:
        app: front
    spec:
      containers:
        - name: front
          image: <IMAGE>
          ports:
            - containerPort: 3000
          env:
          - name: APP_ENV
            value: 'production'
          - name: API_URL
            value: 'https://<API_DOMAIN>'
          imagePullPolicy: Always
      imagePullSecrets:
        - name: regcred
